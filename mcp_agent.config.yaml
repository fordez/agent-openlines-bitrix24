execution_engine: asyncio
logger:
  transports: [console]
  level: info

google:
  default_model: gemini-2.0-flash

openai:
  default_model: gpt-4o

mcp:
  servers: {} # Desactivado: Las herramientas ahora son In-Process (locales) para mayor velocidad
